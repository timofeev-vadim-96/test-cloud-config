server:
  port: 8081
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
    register-with-eureka: true
    #этот сервис не будет обращаться к другим
    fetch-registry: false